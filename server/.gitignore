# Dependencies
node_modules/

# Environment variables
.env
.env.local
.env.*.local
# Production secrets — NEVER commit these; rotate immediately if previously committed
.env.production

# Build output
dist/
build/*.mp4

# Logs
logs/
*.log
*.log.*
npm-debug.log*
yarn-debug.log*
yarn-error.log*
otp-debug.log
pred.log

# WhatsApp session data (Baileys — contains auth tokens, never commit)
whatsapp-auth/
# Legacy wwebjs entries (no longer used)
.wwebjs_auth/
.wwebjs_cache/

# OS files
.DS_Store
Thumbs.db

# Scratch / diagnostic files (local dev only, never commit)
scratch_*.ts
scratch_*.js
scratch_*.cjs
check_*.ts
check_*.js
check_*.cjs

/**
 * DEPRECATED — This file is not mounted and should be deleted.
 *
 * All school routes are handled by school.routes.ts which is mounted at /schools in routes/index.ts.
 * The branches concept was removed in migration 20260314000000_remove_multitenant.
 *
 * Safe to delete this file.
 */
import { Router } from 'express';
const router = Router();
export default router;

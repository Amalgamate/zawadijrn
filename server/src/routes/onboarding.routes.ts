// REMOVED: School onboarding/self-registration — not needed in single-tenant mode
// School setup is done directly via the admin panel or seeding.
import { Router } from 'express';
const router = Router();
export default router;

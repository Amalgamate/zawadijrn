/**
 * MobileAppShell (Deprecated - Mobile UI cleanup)
 * Stub component (not used in current implementation)
 */

import React from 'react';

const MobileAppShell = ({ children }) => {
  return <>{children}</>;
};

export default MobileAppShell;

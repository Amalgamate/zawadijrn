# Backend API Audit — 2026-04-29

## Scope
- Main backend API in `server/src/routes`.
- Control panel API in `platform-console/server.js`.

## Current API Surface
- Base path: `/api` from `server/src/server.ts` and `server/src/routes/index.ts`.
- Route declarations discovered: about 517.
- Method counts:
  - `GET`: 211
  - `POST`: 199
  - `PUT`: 40
  - `PATCH`: 22
  - `DELETE`: 45

## Automated Audit Results
- Frontend/backend mount drift: `npm run audit:api-contract`
  - Result: pass
  - 51 backend mount prefixes
  - 404 frontend files scanned
  - ~504 API references
  - 0 uncovered paths
- Backend response-format audit: `npm --prefix server run audit:responses`
  - Previously broken due to missing script.
  - Fixed by adding `server/scripts/audit-api-response-format.js`.

## Findings
1. Sensitive route exposure (fixed in this change):
   - `diagnostics` and `migrations` were mounted as public routes.
   - They are now mounted after the global `authenticate` gate.
2. Control panel API is still a stub:
   - `POST /api/controls/:action` accepts role-checked requests but does not execute real controls.
3. Response shape consistency is mixed:
   - New audit script reports controller responses without a top-level `success` field.

## Notes
- This audit intentionally does not change business logic in controllers.
- Use `tmp/contract-drift-report.json` and `server/tmp/response-audit.json` as machine-readable outputs.
